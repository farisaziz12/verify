# Neon Postgres — auto-injected on Vercel by the Marketplace integration.
# Locally: paste the connection string from the Neon dashboard (dev branch).
DATABASE_URL=

# Guards /api/cron/sweep. Vercel Cron sends it as `Authorization: Bearer <value>`.
# Generate: openssl rand -hex 16
CRON_SECRET=
