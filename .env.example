# Neon Postgres — auto-injected on Vercel by the Marketplace integration.
# Locally: paste the connection string from the Neon dashboard (dev branch).
DATABASE_URL=

# Optional. When set, the app requires this password and keeps a cookie for one hour.
# Set it on Vercel (Production and Preview) to lock those deployments. Leave empty locally.
SITE_PASSWORD=
